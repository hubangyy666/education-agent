# 关卡、技能分、题目更新与激励语音验收

日期：2026-09-11。目标服务：`http://127.0.0.1:8000`。

## 本次行为

- A1—A10 的 70 个课关、岗关、赛关全部开放。首页仍建议先学基础和复习薄弱课关。
- 技能分为 `答对题数 / 已判定题数 × 10`，保留两位小数。课关保存后即更新；岗/赛整包结算后更新。能力分按题数加权，不混入完成率或诊断分；每次训练每题以最新保存答案为准，返修继承的正确题不重复累计。
- 更新题目优先使用有内容变化的审核储备，缺口有界补充；成功后显示版本、新增与总题数，失败持续显示原因，旧题集仍能练习。进入关卡使用当前发布版本，旧训练仍保留原题和答案。
- 六句 Kokoro 中文神经女声 WAV 随应用提供。答对才播放；AI 回复、错误反馈、讲解不朗读。声音开关保存至当前账号，静音立即停止，切题/路由变化/卸载会取消过期播放。来源与许可见 [voice-feedback.md](voice-feedback.md)。

## 已观察的浏览器行为

使用专属 `qa_agent_learning_ui_0911` 账号，未对四个演示账号提交答案。

1. 全新账号技能图所有能力显示 `0.00分`，A10 的七个按钮均可点击；未完成任何课程，直接进入 A10 赛关，实际生成 10 题训练并显示服务端倒计时。
2. A10-L1 第一道答对后显示技能分 `10.00 / 10 分` 和 `已判定 1 题`。Web Audio 从本地 WAV 解码并启动播放，页面收到播放器 `played` 状态。
3. 第二题答错后显示 `5.00 / 10 分`，未触发激励播放；关闭声音后改正同一题，显示 `10.00 / 10 分`、两题已判定，不重复增加题数，也不播放声音。
4. 刷新页面，恢复第三道未答题，声音仍关闭。独立 PostgreSQL Session 核对 `voice=false`、`answer_count=2`、`correct_count=2`、`skill_score=10.0`、`completed_levels=0`，课堂 Run 仍为 `active`。
5. 重新开启声音并向小基请求提示，实际取得 AI 文字回复；Tutor 组件不再引用语音函数，回答不会触发鼓励播放器。
6. 在实际服务的 A1 页面点击“更新题目”，观察“准备更新”与按钮禁用，随后显示“已发布 V2 题集 · 新增 53 道，共 55 道”。重新进入第一课关，页面实际显示新场景题。独立数据库核对：53 道内容指纹为新题，2 道为复用题；只有一个活动题集；旧 V1 内容与旧 Run 快照均未变化，新 Run 使用 V2 题目。此次 A1 V2 保留为当前发布版本，详见 [learning-update-browser-publication.json](learning-update-browser-publication.json)。

这些观察验证页面、后端判分、实际存储与音频播放调用；个人对音色的喜好可以通过本地样音试听判断。

## 自动验证

- `scripts/verify_encouragement_audio.mjs`：17 项通过。含六个真实 WAV 的字节/哈希、播放器轮换/静音/取消/失败，以及编译后的真实 Vue 组件在延迟 API 响应下的路由/卸载/切题回归。测试环境的 Web Audio 为替身，实际页面播放另见上述浏览器记录。
- 后端测试覆盖 70 关开放、实际 HTTP 提交后的 `3.33 → 6.67`、PostgreSQL 答案往返、用户隔离、岗/赛中途不泄分、声音设置持久化、新版 Run 与旧版快照。
- 最终完整后端测试：**705 项通过，0 失败**，耗时约 66 秒；有 2 条依赖弃用警告。JUnit 记录见 [learning-update-tests.xml](learning-update-tests.xml)。导师固定样本回归使用 V1 独立训练，避免依赖不断更新的活动题集。
- 最终前端 `npm run build` 通过，包括 TypeScript 检查与 Vite 生产构建；构建后的六个音频与源文件哈希一致。
- A1—A10 工厂的真实 API 发布验收全部通过，在独立 PostgreSQL schema 和 Redis 命名空间进行。每个能力的 55 道题分别有 53、44、29、55、47、42、55、55、55、55 道新内容，其余为已发布且未淘汰的复用题，所有关卡均有变化。验收使用此前真实生成并审核的储备，本轮不调用模型，未修改共享活动题集；隔离数据库与缓存已清理。详见 [factory-refresh-all-evidence.json](factory-refresh-all-evidence.json)，A1 单项见 [factory-refresh-A1-evidence.json](factory-refresh-A1-evidence.json)。

## 交付状态

最终前后端代码已由 `http://127.0.0.1:8000` 提供。临时验收账号及其训练、进度与会话在验收后清理，浏览器恢复原演示账号；未对演示账号提交测试答案。用户刷新页面即可加载本次前端。
